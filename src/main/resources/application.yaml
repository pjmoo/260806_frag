spring:
  application:
    name: frag
  profiles:
    active: dev,db
  web:
    error:
      whitelabel:
        enabled: false
---
spring:
  config:
    activate:
      on-profile: dev
    import: optional:file:.env.dev[.properties]
  thymeleaf:
    cache: false